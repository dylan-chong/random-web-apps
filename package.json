{
  "name": "random-web-apps",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "prepare": "git config core.hooksPath .hooks"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
